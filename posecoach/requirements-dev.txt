# Development & load-testing dependencies — NOT installed in the production image.
#   pip install -r requirements-dev.txt
-r requirements.txt

locust==2.32.4            # P09 load test (scripts/load_test.py)
websocket-client==1.8.0   # synchronous WS client used by the InferenceUser
