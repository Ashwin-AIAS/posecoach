# PoseCoach — Python Dependencies
# Install: pip install -r requirements.txt
# Python: 3.11.9 (see .python-version)

# ── Web Framework ──────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9          # Form data parsing

# ── Database ───────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0                  # Async Postgres driver
aiosqlite==0.20.0                # Async SQLite (tests only)
alembic==1.13.2

# ── Cache ──────────────────────────────────────────────────
redis[asyncio]==5.0.8

# ── Auth ───────────────────────────────────────────────────
python-jose[cryptography]==3.3.0  # JWT
passlib[bcrypt]==1.7.4            # Password hashing
slowapi==0.1.9                    # Rate limiting

# ── Computer Vision ────────────────────────────────────────
ultralytics==8.4.51              # YOLO26-Pose (Pose26 class; 8.3.x lacks YOLO26 support)
torch==2.4.1                     # GPU/CPU inference
torchvision==0.19.1
opencv-python-headless==4.10.0.84
numpy==1.26.4

# ── AI / RAG ───────────────────────────────────────────────
google-genai==2.8.0              # Gemini 3.5 Flash (unified Google Gen AI SDK; old google-generativeai retired)
chromadb==0.5.20                 # Vector store
sentence-transformers==3.1.1     # all-MiniLM-L6-v2 embeddings

# ── Logging & Observability ────────────────────────────────
structlog==24.4.0
python-json-logger==2.0.7
prometheus-client==0.21.0
sentry-sdk[fastapi]==2.14.0      # Optional — set SENTRY_DSN to enable

# ── Utilities ──────────────────────────────────────────────
python-dotenv==1.0.1             # Load .env in dev
httpx==0.27.2                    # Async HTTP client (also used in tests)
pydantic[email]==2.9.2           # Request/response validation
anyio==4.8.0                     # >=4.8.0 required by google-genai 2.8.0

# ── Testing ────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-timeout==2.3.1
httpx==0.27.2                    # AsyncClient for FastAPI testing

# ── Linting & Type Checking ────────────────────────────────
ruff==0.6.9
mypy==1.11.2
